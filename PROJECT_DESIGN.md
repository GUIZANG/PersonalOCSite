# PersonalOCSite 设计与开发基线

## 文档职责

本文件记录项目当前有效的产品结构、视觉调性、交互规则与工程约束，是网页设计和优化工作的长期基线。每次修改完成后同步更新受影响的条目，并在维护记录中写明本次形成的最终状态。正文始终描述项目现在的样子，可直接作为后续设计与开发依据。

## 项目定位

PersonalOCSite 是一个以原创角色世界观为核心的网页视觉叙事与交互实验。网站通过空间场景、实验字体、受控信号故障、工业界面和碎片化文本营造意识、监视、记忆、预知与异常档案的体验。

项目的整体气质由以下关键词共同构成：

- 冷峻、神秘、压迫、仪式感与轻度失真。
- 工业诊断界面、档案编号、坐标、刻度、扫描线和信号状态。
- Brutalist 构图与 Swiss Typography 的克制秩序。
- 递归纵深、观察窗口、粒子场、遮罩、故障切片和异质字体。
- 文案承担世界观叙事，界面信息同时承担构图、节奏和沉浸功能。

视觉故障是一种有触发条件、有节奏边界的叙事语言。稳定界面负责建立秩序，故障负责表现观察、侵入、认知偏移与状态转换。

## 体验结构

### Awake / `public/010awake`

网站根路径进入 `awake.html`。场景以黑色空间、地平线、月体、光标反馈和纵向滚动组成意识苏醒序章，滚动阶段依次为 `Void`、`Dazed`、`Lucid`。叙事文字、进度指示和 `Wake Up` 行动共同引导用户进入 Archive。

主色以黑、白和冷光蓝为基础，Credits 展开后进入粉、青、紫构成的独立信号层。字体以 `Cavier` 和 `supplyMono` 为主。背景音乐、滚动反馈和场景动画共同维持缓慢、悬浮、逐步清醒的节奏。

### Precognitive Strata / `public/020archive`

Archive 是当前主实验场景，由 Preloader、Hypercube、Card Stream 和 Credits 四个连续状态组成。

- Preloader 以脑形轮廓、切片进度、神经配准状态与遥测数据建立进入档案层的仪式。加载阶段隐藏系统指针并延后初始化 Archive 自定义指针；脑形切片保持既有加载节奏与左右偏移，仅沿 X 轴归位，故障片段使用不透明深血红形成清晰的异常信号。
- Hypercube 由 Three.js 粒子场、五个可拖动观察窗口、递归黑白纵深框架和边缘 HUD 构成。指针靠近中心后粒子聚合为眼部形态，长按核心持续推进界面失稳，粒子随阶段切换淡出。
- 眼部档案信息采用标题、水平状态基准线与摘要组成的紧凑纵向排版，三者共享左对齐轴和内容宽度。横线位于标题字形下方，摘要紧随轨道排列。标题使用可越过左侧文字边缘的故障切片揭示，四条摘要共享固定高度的文本槽与统一顶端起点，并在横线左端以内通过字符错码、跳变与逐字锁定完成内容切换。状态转换以横向裁切、水平伸缩和左右错位为主：轨道左端固定，白色方块在左端旋转缩小为小正方形，继而拉伸为长方形并转红，随后沿延长的基准线滑动并停靠在设定位置。
- Card Stream 将长按后的界面失稳收束为具备厚度、背壳、材质、反射、红色轮廓光和扫描线的三维档案显示器。模型整体沿纵轴上移 `0.28` 个模型单位，使顶部上表面保持在主要视区之外；深黑背景保持克制，使视觉焦点集中在显示器结构与内容。四个频道为 `SIGNAL`、`VECTOR`、`ORACLE`、`NOESIS`，通过底部轨道拖动、滚轮、方向键和数字键循环切换；`04` 沿当前方向连续旋转进入 `01`，拖拽释放后以受控弹簧速度对齐最近频道。纵向拖动使相机围绕固定观察中心改变垂直轨道角，模型自身保持直立。模型下方提供热成像模式开关，以亮度色阶和短时帧残影呈现另一种实时观察状态。
- Credits 由上边缘驻留触发，观察窗口在纵深连续的条件下重排为致谢索引。底部边缘交互返回 Archive。
- Credits 超链接仅记录当前 Archive 页面中具有可见对应实现的设计来源；GoBold 对应 Card Stream 的旋转显示器运动与热成像观察模式，Utopia Tokyo 对应横向轨道交互，KVS Services 对应动态 ASCII 数字海。
- Credits 页脚与来源列表保持紧凑关联，桌面端上移 `12px`，移动端上移 `8px`，分隔线、版本信息与状态信息作为一个整体对齐。
- Credits 背景以 `11px` 为统一模数：细横线每 `11px` 重复，背景方框在窄屏使用 `44 × 44px`、从 `1366px` 起使用 `55 × 55px`，使每个方框完整容纳四个或五个横线周期并保持边缘对齐。
- Credits 背景字标按七个 SVG 字形的真实可见边界排版：字母宽度与高度均量化为 `11px` 子网格的整数倍，所有字母共享落在主网格边上的顶部与底部基准，首字母左缘和末字母右缘贴合竖向主网格边，相邻字母的可见轮廓间距固定为 `22px`；E 与 T 保持原始字形宽高比例，E 的三条横笔画与 T 的顶部横笔画厚度均等于一个完整背景方框边长，并随主网格在窄屏取 `44px`、宽屏取 `55px`；字标外框始终使用整数列和整数行的背景方框尺寸。
- Card Stream 的方框 Cursor 使用 `4 × 4px` 红色中心方点，与外部直角框保持一致的几何语言；位置、磁吸和交互动画沿用统一的 Cursor 坐标系统。
- Loading Logo 以稳定的暖白主体承载识别，血红色信号残影按组附着在两至三条水平切片上；单次脉冲快速出现、短暂停留并平滑衰减，横向错位控制在 `3–7px`，加载中段密度最高并在完成前恢复稳定。

主色为近黑 `#040405`、暖白 `#ecece7` 与信号红系 `#c51f30` / `#ff2435`，少量青与洋红用于信号串扰。`supplyMono` 承担数据、标记和正文，`Formula` 承担高密度标题与工业显示字。视觉层级依靠亮度、尺度、空间深度和动态频率建立。

Archive 的自定义事件承担模块间状态通信，命名使用 `archive:` 前缀。粒子、观察窗口、递归框架、Card Stream、Credits、预加载器与自定义光标保持独立模块，由语义事件同步阶段状态。

### Black Moon / `public/021blackMoon`

Black Moon 是一组连续的异常文本叙事模块：

1. `010blackMoonText` 以风险声明、逐段解锁、错位文字、复选交互和图像着色器建立“被看见”的心理压迫。
2. `020blackMoonMain` 以预加载文本、ASCII 图像、循环警示带、乱码和显隐切换形成月面异常主体。
3. `030blackMoonTerminal` 以 `CiSecNET-INT` 机要文件数据库的命令行交互收束叙事，并形成返回文本模块的循环。

主色以纯黑、白、暗红和少量青色为主。中文叙事字体、等宽终端字与故障展示字体按信息角色并置。交互节奏由阅读、确认、悬停暴露、故障跳转和键盘输入逐级推进。

### 研究与实验目录

`public/reference`、`public/test` 和 `public/dither` 保存视觉与交互研究，不属于网站主叙事路径。可复用成果在进入正式场景后遵循正式场景的命名、状态管理、响应式与性能约束。

## 统一设计语言

### 构图

- 使用满屏场景、边缘轨道、角标、刻度、窄栏文本和中心视觉对象建立空间秩序。
- 留白承担压迫感与尺度感，信息密度集中在边缘、控制节点和状态切换区域。
- 装饰信息与可交互控件在视觉上同源，在语义、焦点状态和指针反馈上保持可辨识。
- 层级优先通过空间、字号、字重、透明度和运动建立，边框与发光作为精确提示。

### 色彩

- 每个场景维持自身受控色域；新增颜色需要具有明确的状态或叙事含义。
- 黑与近黑构成主要空间，白与灰构成结构信息，高饱和色用于信号、危险、异常和转换。
- Archive 以暖白和信号红为识别核心；Awake 以冷蓝光感为核心；Black Moon 以暗红警告和黑白文本冲突为核心。
- 共享色值优先使用 `public/styles/colors.css` 中的语义变量，场景专属状态色保留在场景样式内。

### 字体与文案

- `supplyMono` 用于档案数据、坐标、状态、控制提示和小字号界面文本。
- `Formula` 用于 Archive 的高压缩标题、频道名和工业显示结构。
- `Cavier` 用于 Awake 的轻量叙事与行动文字。
- 中文展示字体与故障字体服务于 Black Moon 的段落身份和异常层级。
- 文案采用档案编号、状态短语、技术术语和碎片叙事，保持简短、准确、具有世界内身份。
- 界面文字直接存在于叙事世界中，标题、按钮和提示描述当前动作或状态。

### 动效与交互

- 常态运动克制、连续，关键转换具有明确的蓄力、临界点和释放阶段。
- 故障效果围绕触发、峰值与恢复组织，避免持续占据所有层级。
- 指针、拖动、滚动、长按、驻留和键盘输入均提供与场景一致的即时反馈。
- 指针旁的交互状态标识使用整数坐标直接排版，保持原有字体与字号，并避免文字进入持续变换的合成图层，以确保小尺寸字符边缘清晰稳定。
- 跨状态转换保持视觉对象的空间连续性，尤其关注观察窗口、中心对象和前后景深度。
- 移动端、触控端和键盘输入保留核心路径；视觉复杂度可响应设备条件，叙事结果与关键节奏保持一致。
- 支持 `prefers-reduced-motion` 的组件提供稳定、清晰的低动态状态。

## 工程结构

- Vite 负责开发与构建，根路径跳转至 Awake。
- 正式体验采用 `public` 下的静态多页面 HTML、CSS 与 JavaScript；各场景可独立加载和开发。
- Three.js / WebGL 负责粒子、着色器、三维模型与空间背景，GSAP 和 anime.js 负责选定的时间轴与补间。
- SVG、Canvas、CSS 遮罩、混合模式和裁切路径负责网格、字标、扫描、切片与界面故障。
- `src` 保留 Vue 脚手架，不承载当前正式场景。
- `public/libs` 为本地浏览器依赖，`public/assets` 为共享字体、图像、音频、模型和纹理。

## 修改与交付约束

- 修改范围以用户当前明确需求为边界，只补充让目标完整成立的代码、样式、内容和验证。
- 视觉与交互修改先识别所属场景、状态源和共享依赖，在负责该行为的源头完成一致性修复。
- 同一状态由单一逻辑负责，关联模块通过既有状态接口或语义事件同步。
- 更新现有实现时直接整理为当前正确结构，清理重复规则、互相覆盖的状态和失效分支。
- 保持既有命名语义、分层方式、资源组织和场景色域；新增共享抽象以真实复用为前提。
- 性能优化保持粒子数量、构图、时间、缓动、色彩和交互结果等用户可感知设计成立。
- 修改后的页面在目标路径、主交互、常见视口和生产构建中完成验证。
- 最终页面、代码、注释、文件名和说明只表达当前有效结果。

## 文档维护方式

每次网页修改完成后执行以下维护：

1. 更新受影响的体验结构、设计语言或工程规则，使正文与当前实现一致。
2. 在维护记录中新增一条日期、范围和最终状态；粒度以一个可验证的设计或功能结果为单位。
3. 同一主题再次调整时，以当前结果重写对应条目，使记录保持单一、连贯、可直接执行。
4. 构建、测试和兼容信息仅在会影响后续维护时进入正文。

## 维护记录

### 2026-08-24 · 项目设计基线

- 项目结构、三组正式叙事场景、研究目录与页面流转关系形成统一说明。
- 视觉调性、场景色域、字体职责、动效原则、交互原则和工程边界形成后续网页设计与优化基线。
- 项目修改采用范围内交付、源头一致性修复、最终状态记录和生产构建验证。

### 2026-08-24 · Archive 眼部档案排版

- `THE EYE` 标题、水平状态基准线与摘要使用统一左边界和内容宽度；横线位于标题下方，摘要采用紧凑行距与自适应高度。
- `THE EYE`、水平线、摘要与 `00` 深度区域使用统一的左右式状态转换，横线保持固定高度与固定左端进行水平伸缩。
- 轨道信号使用单一组件连接两种状态：白色方块以自身中心为轴完成一次短促旋转，并在旋转过程中等比缩小，随后连贯拉伸为白色长方形，原位转红后沿线滑动，最终以右端对齐横线长度的 80% 位置。整体节奏紧凑，前段形变快速连贯，沿线位移以较高初速度启动并在接近停靠点时集中减速。每次指针进入观察核心均完整播放正向动画，离开时直接反向播放同一动画实例，从当前进度连续恢复为白色方块。方块中心与长条横向中轴始终对齐水平基准线，并与横线延展保持同步。
- 待机状态下，白色方块与白色进度条共享同一轨道宽度、进度坐标和水平基准线，白色进度段直接连接方块，方块停留在当前 Scrub 深度对应的位置并随拖动同步更新。进入观察核心时，白色进度段始终连接信号组件并同步改变长度；信号组件以先慢后快的加速节奏回到轨道起点，再从起点连续播放旋转缩小、横向拉伸、转红与减速滑行的完整时间线。离开观察核心后，白色进度段随信号反向运动，组件以先快后慢的减速节奏恢复到当前 Scrub 深度位置。动画期间进度段保持既有颜色、粗细与亮度。
- 桌面端与移动端共享同一排版和动效方向，标题故障效果使用独立捕获留白。

### 2026-08-24 · Credits 网格字标

- Credits 背景使用 `11px` 细线模数与 `44px` / `55px` 主方框网格，字标外框、字母边界和轮廓间距共同落在统一模数上。
- `CREDITS` 七个字形共享顶部与底部基准，相邻轮廓保持 `22px` 间距；E 与 T 沿用原始字宽比例，横笔画厚度跟随当前主方框边长。

### 2026-08-24 · Archive 加载动效

- Loading 阶段保持无指针界面，Archive 自定义指针在加载完成后初始化。
- 脑形切片保持原有进度、故障与退场节奏，位移仅使用 X 轴；故障片段以不透明深血红显示。

### 2026-08-29 · Credits 来源索引

- Credits 来源与 Archive 当前可见实现保持一一对应，GoBold 记录 Card Stream 旋转显示器与热成像模式参考，Utopia Tokyo 记录横向轨道交互参考，KVS Services 记录动态 ASCII 数字海参考。
- Credits 页脚在不改变来源列表排布的条件下靠近超链接区域，桌面端与移动端分别使用 `12px` 和 `8px` 的上移距离。

### 2026-08-29 · Card Stream 空间背景

- 长按完成后，Hypercube 与眼部粒子随阶段过渡自然淡出，三维显示器进入纯净的深黑空间。
- Credits 来源索引保持与当前可见设计对应，来源条目按页面中的实际视觉与交互结构维护。

### 2026-08-30 · Card Stream 循环旋转

- 三维显示器采用连续循环坐标，拖拽可沿同一方向从 `04` 自然进入 `01`，同时保持频道索引按四个状态循环。
- 松开指针时直接锁定连续坐标中的最近绝对角度，轨道弹簧与模型旋转分别推进；模型使用首尾速度均为零的单调缓动对齐频道，释放过程保持当前方向且不注入额外初速度。

### 2026-08-30 · Card Stream 纵向拖拽响应

- 横向拖拽独立控制频道旋转；纵向位移以 `0.00045rad/px` 的灵敏度同向改变相机垂直轨道角，鼠标向上时观察高度上升、向下时观察高度下降，观察范围限制在默认视角上下 `6°`。相机沿以模型上部主体为中心的球面轨道运动并持续朝向该中心，使透视、表面可见范围与反射随观察高度共同变化，模型本体保持直立。
- 释放指针后，相机以低速阻尼弹簧返回默认观察角度；模型顶部结构保持既有平行关系，频道对齐运动不受视角回正影响。
- 模型根节点在基准高度上增加 `0.28` 个模型单位的纵向显示偏移，保持主体构图稳定并收束顶部上表面的可见范围。

### 2026-08-30 · Card Stream 热成像模式

- 热成像作用于 3D 模型与动态数字海合成后的完整 WebGL 场景，频道轨道、文字、按钮与 Cursor 保持原有视觉状态；模式默认关闭，通过模型下方的热感应模块标签切换。标签在待机状态使用无描边的切角黑色底板、固定高度的左侧红色能量刻度和 `T–00 / T–01` 状态编码，激活时刻度增强亮度与辉光并显示血红色轮廓，同时以一次短促的水平信号扰动完成状态确认。Cursor 进入标签时，原有四角标记从 Cursor 外框连续移动并扩展至按钮四角，不经过透明度切换；中心红色方点旋转缩小并移动至按钮左下边缘，展开为 `16×1px` 的水平信号槽。吸附状态保持稳定，离开时信号槽与角标沿相同路径反向恢复。
- 色阶由画面亮度驱动，依次经过黑、深蓝、紫、红、橙、黄与白热，并保留现有扫描线、颗粒和信号柔化层。
- 每次开启 Thermal 时生成独立故障种子，完整场景随机划分为八至十二组带轻微高度差的水平信号切片，每组再形成三至七个横向片段。种子统一改变切片边界、横向分段、接管顺序、错位方向和连闪时机；切换阶段产生 `3–12px` 的短时横向错位、离散普通/热成像跳变和轻微色彩分离，覆盖完成后全部切片归位。拉丝由模型与数字海的自然运动经过帧历史合成形成，不规定固定方向或人为拉伸长度。历史帧仅保留单通道值高于 `0.1` 的亮部，并以 `0.93` 阻尼与当前帧逐通道取最大值，使高温轮廓和数字波峰留下清晰轨迹，暗部持续回落至纯黑。
- 关闭时同一组信号切片反向恢复普通画面并停止历史帧累积。稳定的普通模式与 Thermal 模式均保持画面归位；减少动态效果模式保留热成像色阶并停止帧历史合成。

### 2026-08-30 · Loading 双侧信号频谱

- Archive Loading 背景采用双侧信号频谱：十九条长度、亮度和呼吸周期各异的水平信号线分布于脑形 Logo 左右，中央基准线保持主体与外围信息的横向联系。频谱位于全屏退出切片之上、脑形主体与加载信息之下，加载期间保持完整可见。
- 血红异常线与脑形 Logo 的故障脉冲同步出现；频谱在加载退出时随背景淡出，减少动态效果模式保持稳定线长。
- `loading-concepts.html` 保留档案坐标矩阵、双侧信号频谱和神经拓扑轨迹三套完整动态方案，支持点击进入全屏观察。

### 2026-08-30 · Thermal Cursor 底边信号槽

- Thermal Cursor 吸附后将中心方点转化为按钮左下边缘的水平信号槽，信号槽与左侧垂直能量刻度保持分离，构成清晰的双层状态结构。
- 信号槽在 Hover 期间固定于按钮底边，离开时反向收束为 Cursor 中心方点；按钮点击与 Card Stream 拖动保持独立。

### 2026-08-31 · Hypercube 首次悬停门控

- Loading 完成时记录物理指针位置；指针若已位于 Hypercube 交互半径内，当前重叠保持 `DORMANT`，不触发 Hover、视角跟随、Cursor 吸附或长按注册。
- 初始门控使用独立解除半径，在 Hover 进入半径外保留 0.025 的轻微缓冲；指针一次移出该范围即解除，下一次进入按正常 Hover 逻辑运行。Loading 完成时位于交互区域外的指针直接使用正常交互。

### 2026-08-31 · Hypercube 至 Card Stream 数据海转化

- Card Stream 背景通过相机射线与模型下方水平面的交点生成视觉无限的动态数字海。数字海 Canvas 从模型操作区延伸至完整视口底边，与底部轨道的预留空间保持独立。数字海随 Card Stream 相机实时重算透视，纵向拖动改变观察高度时保持水平空间关系；流向取相机在水平面上的近景方向，血红字符单元与红黑波场稳定向画面下方流动，远景逐步收束为黑色。
- 数字海使用域扭曲的五层分形噪声统一生成低频地貌、中频破碎场和高频边缘细节。页面每次刷新生成一次场景种子，统一改变红黑地貌、边缘细节和初始字符组合。黑色作为完整底面，红色波峰以单层覆盖方式显现，窄幅明亮前沿塑造不规则侵蚀轮廓。字符单元使用彼此独立的随机保持时长，并在少量更新周期内产生短促连闪，形成离散故障式切换；减少动态效果模式固定波场与字符时间。
- 眼睛长按完成后展开为带透视的数据海桥接面。保留粒子按固定步长排列为字符网格，眼形由中央向左右和景深方向铺开，方点在运动中转化为字符；其余粒子随网格形成过程收束。Card Stream 读取同一 Burst 进度，在桥接面形成后显现真实水平数据海和三维模型。
- 数据海与模型分别渲染并合成为完整场景，再统一进入 Thermal 色阶与帧历史处理。入场前段沿眼睛中心向外展开，完成时覆盖完整视野并保持无可见几何边界。Thermal 切换通过随机水平信号切片完成全场景接管与恢复，模型与数字海共同进入冷黑、深蓝、紫、红、橙、黄和白热色阶。帧历史作用于完整热成像场景，WebGL 之外的按钮、Cursor、频道文字和底部轨道保持原有色彩与清晰度。入场阶段统一锁定拖拽、滚轮、键盘和 Thermal 控件，模型与地面稳定后开放完整交互。

### 2026-08-31 · 连续数字海独立预览

- `data-sea-concept.html` 提供全屏数字海视觉验证页。页面通过相机射线与水平面的交点生成无可见边界的数据平面，字符单元在红色区域连续铺展，并与色块波场同步向画面下方流动。每次刷新生成独立场景种子；字符内容按单元独立的随机周期更新，少量周期包含快速连闪，形成离散故障节奏。
- 红黑色阶由域扭曲的五层分形噪声场统一生成。低频地貌、中频破碎场和少量高频细节沿共同方向缓慢迁移，形成尺度不一、轮廓不规则的连续波峰与波谷。黑色作为完整底面，红色区域以单层覆盖关系显现；窄幅高频扰动与明亮红色前沿共同塑造细碎、清晰的侵蚀边缘。红色区域承载字符，黑色波谷保持纯黑。
- 预览页使用固定相机与远景距离收束，集中呈现字符流和不规则颜色波浪的关系；减少动态效果模式固定时间并保留完整空间结构。
